import MiniDrawer from "../components/MiniDrawer";

type Props = {};

export default function Home({}: Props) {
    return (
        <div>
            <MiniDrawer>
                <div>Home</div>
            </MiniDrawer>
        </div>
    );
}
